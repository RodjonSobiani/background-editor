# Default text:

# background-editor

## Quick setup

If you've done this kind of thing before:

- **Set up in Desktop** or use:
    - HTTPS: `https://github.com/RodjonSobiani/background-editor.git`
    - SSH: `git@github.com:RodjonSobiani/background-editor.git`

## Get started

Create a new file or upload an existing file. We recommend every repository include a:
- README
- LICENSE
- .gitignore

## …or create a new repository on the command line

```bash
echo "# background-editor" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/RodjonSobiani/background-editor.git
git push -u origin main
```

## …or push an existing repository from the command line
```bash
git remote add origin https://github.com/RodjonSobiani/background-editor.git
git branch -M main
git push -u origin main
```
